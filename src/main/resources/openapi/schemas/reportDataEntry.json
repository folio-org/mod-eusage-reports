{
  "description": "report data entry",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "report data identifier"
    },
    "kbTitleId": {
      "type": "string",
      "description": "kb title identifier (if the agreement is a title)"
    },
    "type": {
      "type": "string",
      "description": "one of journal, pacakge, ebook"
    },
    "counterreporttitle": {
      "type": "string",
      "description": "counter report title"
    },
    "agreementLineId": {
      "type": "string",
      "description": "agreement identifier"
    },
    "encumberedCost": {
      "type": "number",
      "description": "cost from the PO line"
    },
    "invoicedCost": {
      "type": "number",
      "description": "Total access count"
    }
  },
  "additionalProperties": false,
  "required": [
    "id", "agreementLineId"
  ]
}
